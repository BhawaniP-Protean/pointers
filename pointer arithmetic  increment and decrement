#include<stdio.h>
int main(){
    int a[5]={10,20,30,40,50};
    int *p;
    p=&a[0];
    printf("%d %d",*p++,*++p);  in increment or decrement operator it will do the operation from right to left but execute from left to right...

    printf("%d",*p--);


  }
  
  // output
  20 20
  30
  
